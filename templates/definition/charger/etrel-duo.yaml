template: etrel-duo
products:
  - brand: Etrel
    description:
      generic: INCH Duo
capabilities: ["mA"]
params:
  - name: connector
  - name: host
  - name: port
    default: 502
render: |
  type: etrel
  connector: {{ .connector }}
  uri: {{ .host }}:{{ .port }}
