template: etrel
products:
  - brand: Etrel
    description:
      generic: INCH
  - brand: Sonnen
    description:
      generic: sonnenCharger
capabilities: ["mA"]
params:
  - name: host
  - name: port
    default: 502
render: |
  type: etrel
  uri: {{ .host }}:{{ .port }}
